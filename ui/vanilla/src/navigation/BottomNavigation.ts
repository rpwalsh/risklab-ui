import { UIElement } from '../core/UIElement'; import { register } from '../core/register';
export class UIBottomNavigation extends UIElement { static observedAttributes=['value','label','show-labels']; protected styles():string{return `:host{display:block}nav{display:flex;justify-content:space-around;border-top:1px solid var(--ui-color-border,#cbd5e1);background:var(--ui-color-surface);padding:.25rem}::slotted(ui-side-nav-item){flex:1;text-align:center}`;} protected template():string{return `<nav aria-label="${this.getAttr('label','Bottom navigation')}"><slot></slot></nav>`;} } register('ui-bottom-navigation',UIBottomNavigation);
export class UILink extends UIElement { static observedAttributes=['href','target','underline','disabled']; protected styles():string{return `:host{display:inline}a{color:var(--ui-color-primary,#4f46e5);text-decoration:var(--_decoration,none);text-underline-offset:2px}:host([underline=always]){--_decoration:underline}:host([underline=hover]) a:hover{text-decoration:underline}:host([disabled]){pointer-events:none;opacity:.5}`;} protected template():string{return `<a href="${this.getAttr('href','#')}" target="${this.getAttr('target')}" ${this.getBoolAttr('disabled')?'aria-disabled="true"':''}><slot></slot></a>`;} } register('ui-link',UILink);

import { UIElement } from '../core/UIElement'; import { register } from '../core/register';
export class UISearchInput extends UIElement { static observedAttributes=['label','placeholder','value','disabled','clearable']; protected styles():string{return `:host{display:block}.control{display:flex;align-items:center;gap:.45rem;padding:0 .65rem;border:1px solid var(--ui-color-border,#cbd5e1);border-radius:.5rem;background:var(--ui-color-surface)}input{flex:1;min-width:0;padding:.6rem 0;border:0;outline:0;background:transparent;color:var(--ui-color-text);font:inherit}button{all:unset;cursor:pointer;color:var(--ui-color-text-secondary,#64748b)}`;} protected template():string{return `<div class="control"><span aria-hidden="true">⌕</span><input type="search" aria-label="${this.getAttr('label','Search')}" placeholder="${this.getAttr('placeholder')}" value="${this.getAttr('value')}" ${this.getBoolAttr('disabled')?'disabled':''}/>${this.getBoolAttr('clearable')?'<button type="button" aria-label="Clear search">×</button>':''}</div>`;} protected onRendered():void{const input=this.$<HTMLInputElement>('input');input?.addEventListener('input',()=>this.emit('ui-input',{value:input.value}));this.$('button')?.addEventListener('click',()=>{if(input){input.value='';input.focus();this.emit('ui-input',{value:''});}});} } register('ui-search-input',UISearchInput);

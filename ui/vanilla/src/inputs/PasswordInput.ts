import { UIElement } from '../core/UIElement'; import { register } from '../core/register';
export class UIPasswordInput extends UIElement { static observedAttributes=['label','placeholder','value','disabled','readonly','show-strength']; private visible=false; protected styles():string{return `:host{display:grid;gap:.35rem}label{font-size:.875rem;font-weight:600}.control{display:flex;border:1px solid var(--ui-color-border,#cbd5e1);border-radius:.5rem;background:var(--ui-color-surface)}input{flex:1;min-width:0;padding:.6rem .7rem;border:0;outline:0;background:transparent;color:var(--ui-color-text);font:inherit}button{all:unset;padding:0 .65rem;cursor:pointer;color:var(--ui-color-text-secondary,#64748b)}.strength{height:3px;background:var(--ui-color-border)}.strength span{display:block;height:100%;width:var(--_strength);background:var(--_tone)}`;} protected template():string{const value=this.getAttr('value'),strength=Math.min(100,value.length*12+( /[A-Z]/.test(value)?15:0)+( /\d/.test(value)?15:0));return `<label>${this.getAttr('label')}</label><div class="control"><input type="${this.visible?'text':'password'}" placeholder="${this.getAttr('placeholder')}" value="${value}" ${this.getBoolAttr('disabled')?'disabled':''} ${this.getBoolAttr('readonly')?'readonly':''}/><button type="button" aria-label="${this.visible?'Hide':'Show'} value">${this.visible?'Hide':'Show'}</button></div>${this.getBoolAttr('show-strength')?`<div class="strength" aria-label="Strength ${strength}%"><span style="--_strength:${strength}%;--_tone:${strength>70?'var(--ui-color-success)':strength>40?'var(--ui-color-warning)':'var(--ui-color-error)'}"></span></div>`:''}`;} protected onRendered():void{const input=this.$<HTMLInputElement>('input');input?.addEventListener('input',()=>{this.setAttribute('value',input.value);this.emit('ui-input',{value:input.value});});this.$('button')?.addEventListener('click',()=>{this.visible=!this.visible;this.render();this.$<HTMLInputElement>('input')?.focus();});} } register('ui-password-input',UIPasswordInput);

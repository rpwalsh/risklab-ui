import { UIElement } from '../core/UIElement'; import { register } from '../core/register';
export class UIDescriptionItem extends UIElement { static observedAttributes=['term']; protected styles():string{return `:host{display:grid;grid-template-columns:minmax(8rem,35%) 1fr;gap:.75rem;padding:.5rem 0;border-bottom:1px solid var(--ui-color-border,#cbd5e1)}dt{color:var(--ui-color-text-secondary,#64748b);font-size:.8rem}dd{margin:0;font-size:.875rem}`;} protected template():string{return `<dt>${this.getAttr('term')}</dt><dd><slot></slot></dd>`;} } register('ui-description-item',UIDescriptionItem);
export class UIDescriptionList extends UIElement { static observedAttributes=['label']; protected styles():string{return `:host{display:block}dl{margin:0}`;} protected template():string{return `<dl aria-label="${this.getAttr('label','Details')}"><slot></slot></dl>`;} } register('ui-description-list',UIDescriptionList);

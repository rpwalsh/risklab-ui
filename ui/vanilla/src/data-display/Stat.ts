import { UIElement } from '../core/UIElement'; import { register } from '../core/register';
export class UIStat extends UIElement { static observedAttributes=['label','value','delta','trend','unit','compact']; protected styles():string{return `:host{display:block}.stat{display:grid;gap:.2rem}.label{color:var(--ui-color-text-secondary,#64748b);font-size:.75rem;text-transform:uppercase;letter-spacing:.05em}.value{font-size:var(--_size,1.8rem);font-weight:700;line-height:1.1}:host([compact]){--_size:1.25rem}.delta{font-size:.78rem;color:var(--ui-color-text-secondary,#64748b)}:host([trend=up]) .delta{color:var(--ui-color-success,#22c55e)}:host([trend=down]) .delta{color:var(--ui-color-error,#ef4444)}`;} protected template():string{return `<div class="stat"><span class="label">${this.getAttr('label')}</span><span class="value">${this.getAttr('value')}<small>${this.getAttr('unit')}</small></span><span class="delta">${this.getAttr('trend')==='up'?'↑':this.getAttr('trend')==='down'?'↓':'→'} ${this.getAttr('delta')}</span></div>`;} } register('ui-stat',UIStat);

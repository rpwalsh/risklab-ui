import { UIElement } from '../core/UIElement'; import { register } from '../core/register';
export class UIAvatarGroup extends UIElement { static observedAttributes=['spacing','max','label']; protected styles():string{return `:host{display:inline-flex}.group{display:flex;align-items:center}::slotted(ui-avatar){margin-left:var(--_spacing,-.55rem);box-shadow:0 0 0 2px var(--ui-color-surface)}::slotted(ui-avatar:first-child){margin-left:0}`;} protected template():string{return `<div class="group" role="group" aria-label="${this.getAttr('label','People')}" style="--_spacing:${this.getAttr('spacing','-.55rem')}"><slot></slot></div>`;} } register('ui-avatar-group',UIAvatarGroup);

import { UIElement } from '../core/UIElement'; import { register } from '../core/register';
export class UICenter extends UIElement { static observedAttributes=['inline','min-height']; protected styles():string{return `:host{display:block}.center{display:grid;place-items:center;min-height:var(--_min,auto)}:host([inline]){display:inline-block}`;} protected template():string{return `<div class="center" style="--_min:${this.getAttr('min-height','auto')}"><slot></slot></div>`;} } register('ui-center',UICenter);
export class UISpacer extends UIElement { static observedAttributes=['size']; protected styles():string{return `:host{display:block;flex:1 1 auto}.spacer{min-width:var(--_size,0);min-height:var(--_size,0)}`;} protected template():string{return `<span class="spacer" style="--_size:${this.getAttr('size','0')}"></span>`;} } register('ui-spacer',UISpacer);
